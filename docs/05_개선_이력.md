# 개선 이력 — 무엇을, 왜, 어떻게 바꿨고 얼마나 좋아졌나

이 문서는 **보고자료의 원천**입니다. 도구를 바꿀 때마다 아래 네 칸을 채운 항목을 하나씩 추가합니다.

| 칸 | 적는 것 |
|---|---|
| 개선 방법 | 무엇을 어떻게 했나 (한 줄) |
| 개선된 부분 | 어느 파일·화면·절차가 바뀌었나 |
| 개선 이유 | 어떤 문제·요구 때문에 했나 |
| 개선 결과 (정량) | 전후 숫자. **측정**한 값과 **추정**한 값을 구분해 적는다 |

날짜는 한국 시간 기준. 버전은 화면 제목 옆에 찍히는 `config.VERSION` 값이고, 저장소에 올릴 때마다 올립니다.
수치가 "측정" 이면 어디서 쟀는지(예시 문서 5장 / 회사 PC 실행 기록) 를 함께 적었습니다.

> 검토 반영으로 **판정 정확도**가 어떻게 변했는지는 이 문서가 아니라 화면의 [성능 기록] 탭이 회차마다 자동 기록합니다
> (재현율·정밀도·F1·등급일치, 최초 설정 대비 Δ). 보고 때는 그 탭의 [보고용 표 복사] 로 표를 가져오세요 (`docs/04_성능_측정.md`).
> 이 문서는 **프로그램 자체의 개선**(속도·안정성·사용성·산출물)을 다룹니다.

---

## 한눈에 보기

| # | 날짜 | 개선 | 정량 결과 (요약) |
|---|---|---|---|
| 1 | 08-16 ~ 08-24 | 도구 완성·공개 (읽기→규칙 사전→모델 판정→병합→마킹) | 5단계 파이프라인, 규칙 30여 개, 카테고리 11종 |
| 2 | 09-18 | 기본 모델 qwen3:14b → **qwen3:8b** | 모델 9.3GB → 5.2GB, 토큰당 메모리 읽기 −44% (추정 속도 ×1.8) |
| 3 | 09-19 | **검토 반영** + **성능 측정**(A/B 채점) 신설 | 검토완료본 1개 → 오탐·누락이 사전·프롬프트에 자동 반영, 지표 4종 자동 산출 |
| 4 | 09-19 | 14b 경로 정리, 오프라인 꾸러미 축소 | 꾸러미 15GB → 약 6GB (−60%) |
| 5 | 09-19 | 검토완료본 복수 업로드 | 파일 N개를 한 번에 (파일별 독립 처리) |
| 6 | 09-19 | 변경 일지·개선 타임라인 | 반영 1회마다 전후 기록 자동 (수동 기록 0) |
| 7 | 09-19 | 표시 방식: 구간 문구 → 첫 슬라이드 범례 | 원본에 덧붙는 요소: 구간마다 1개 → 문서당 1개 |
| 8 | 09-20 | 예시 선별 개선·교정 데이터 보호 | 주입 예시 반례 4 + 정답 8, 보관 상한 12 → 무제한 |
| 9 | 09-20 | 업데이트 절차 [7-1], Windows 콘솔 출력 안정화 | 업데이트 5분 절차 문서화, cp949 출력 오류 0 |
| 10 | 09-20 | 산출물 간소화 (배지·요약 슬라이드·발표자 노트 제거) | 원본 변경 요소 4종 → 2종 (형광펜·범례) |
| 11 | 09-21 | **파일 입력 4가지** + `mark.bat` (업로드 차단 PC 대응) | 입력 통로 1 → 4 + 명령행, 점검 38건 |
| 12 | 09-21 | `setup.bat` 회사망 대응 (SSL 재시도, pip 대기 120초) | 설치 실패 시 재시도 1회 자동, pip 대기 15 → 120초 |
| 13 | 09-21 | **슬라이드당 10분 제한 제거** + 즉시 중단·서버 감지 | 제한 600초 → 없음, [중단] 반응 ~40초 → ~3초, 서버 사망 감지 ≤45초 |
| 14 | 09-21 | 버전 표시·첫 화면 캐시 금지·오류 문구에 단계/버전 | 버전 확인: 테스트 1분 → 화면 즉시 |
| 15 | 09-21 | **모델 답 다이어트** + 속도 기록 + 특허 행정 오탐 필터 | 출력 토큰 −45%, 쓰기 시간 −47%, 오탐 3 → 0 (측정) |
| 16 | 09-21 | 화면 **모델 선택: 정밀(8b) / 빠름(4b)** + `--fast` + setup 이 둘 다 설치 | 빠름 = 약 3.3배 빠름(측정), 후보 1/3 감소·A 등급 없음 → 초벌용 |

---

## 회차별 상세

### 1. 도구 완성과 공개 (2026-08-16 착수 → 08-24 GitHub 공개) — 버전 없음

- **개선 방법**: PPTX 보고자료에서 특허 출원 후보 구간을 찾아 형광펜으로 표시하는 도구를 만듦.
  읽기(extract) → 규칙 사전(lexicon) → 온디바이스 모델 판정(analyze, Ollama+Qwen3) → 병합(merge) → 마킹(mark) 5단계.
- **개선된 부분**: `app/` 전체, 웹 화면, 설치·실행 스크립트(Windows/Mac), 안내 문서 3편, 예시 문서·결과.
- **개선 이유**: 보고자료는 결과 위주라 발명이 문장에 빠져 있는 경우가 대부분 → "수단이 존재함을 시사하는 표현"까지 잡아
  변리사에게 넘길 후보를 빠짐없이 추리기 위해. 인터넷 없이, 문서가 PC 밖으로 나가지 않는 조건.
- **개선 결과**: 규칙 30여 개·카테고리 11종·등급 A/B/C. 통신은 127.0.0.1(같은 PC 의 Ollama)만. 외부 요청 0건.

### 2. 기본 모델 qwen3:14b → qwen3:8b (2026-09-18)

- **개선 방법**: 기본 모델을 8b 로 낮추고 14b 실행 파일(`run_14b.bat`)을 없앰. 업그레이드는 `PM_MODEL` 환경 변수로.
- **개선된 부분**: `app/config.py` MODEL, 루트 스크립트, README, 안내 문서, 점검 스크립트.
- **개선 이유**: 회사 PC 에 GPU 가 없고 RAM 16GB 로 확정. 14b(9.3GB)는 메모리에 겨우 올라가고 CPU 에서 너무 느림.
- **개선 결과**: 모델 파일 9.3GB → 5.2GB(−44%). CPU 에서 토큰당 속도는 메모리 읽기량에 반비례하므로 **약 1.8배 빠름(추정, 이론치)**.
  실측은 회사 PC `review_data\run_log.tsv` 의 "쓰기토큰/초" 로 확인.

### 3. 검토 반영 + 성능 측정 (2026-09-19)

- **개선 방법**: 사람이 형광펜을 교정한 **검토완료본**을 올리면 도구의 판정과 비교해 오탐(빼야 할 것)·누락(넣어야 할 것)을 추출,
  **규칙 사전과 프롬프트 예시에 자동 반영**. 그리고 확정된 정답을 문제지 삼아 **최초 설정 vs 현재 설정을 같은 문제로 A/B 채점**.
- **개선된 부분**: `app/review.py`(신설), `app/evaluate.py`(신설), 화면 [검토 반영]·[성능 기록] 탭, `docs/03`, `docs/04`, 점검 스크립트.
- **개선 이유**: 도구가 쓸수록 좋아져야 하고, "얼마나 좋아졌는지" 를 회사에 숫자로 보고해야 함. GPU 가 없어 파인튜닝은 불가 →
  데이터(교정 내역)를 쌓아 사전·프롬프트로 반영하는 길을 택함.
- **개선 결과**: 검토완료본 1개 → 교정 내역 자동 추출·반영 (수작업 0). 지표 4종(재현율·정밀도·F1·등급일치) + 최초 대비 Δ 자동 산출,
  회차마다 `review_data/eval_history.jsonl` 누적. **실제 수치는 회사 PC [성능 기록] 탭에 쌓임** — 보고 때 [보고용 표 복사].
  (해석 원칙: 문제지가 회차마다 자라므로 회차 간 절대값보다 **같은 회차 안의 최초→현재 Δ** 를 개선 폭으로 읽는다.)

### 4. 14b 경로 정리, 오프라인 꾸러미 축소 (2026-09-19)

- **개선 방법**: GPU 확보 불가 확정에 따라 저장소·문서·꾸러미 스크립트에서 14b/고품질 경로를 정리.
- **개선된 부분**: `tools/` 꾸러미 스크립트, 문서, README.
- **개선 이유**: 두 갈래 안내가 초보 사용자를 헷갈리게 하고, 꾸러미가 USB 용량을 넘김.
- **개선 결과**: 오프라인 꾸러미 15GB → 약 6GB(−60%).

### 5. 검토완료본 복수 업로드 (2026-09-19)

- **개선 방법**: 여러 검토완료본을 한 번에 올려 파일별로 독립 처리. 깨진 파일은 그 파일만 제외, 같은 문서 두 벌은 첫 벌만.
- **개선된 부분**: `review.preview_many()`, 화면 [검토 반영] 탭, 점검 6건.
- **개선 이유**: 검토는 한 번에 여러 문서가 돌아오는데 하나씩 올리는 게 번거로움.
- **개선 결과**: 업로드 N회 → 1회. 오류 파일이 나머지 파일의 반영을 막지 않음.

### 6. 변경 일지·개선 타임라인 (2026-09-19)

- **개선 방법**: [반영 저장] 때 데이터·주입 예시·사전 개수의 **전후**를 `review_data/change_log.jsonl` 에 기록하고,
  [성능 기록] 탭에 성능 측정 결과와 시간순으로 병합해 보여 줌. [보고용 표 복사]에 포함.
- **개선된 부분**: `review.commit_with_log()`, 화면 타임라인, `docs/03`·`04`.
- **개선 이유**: "무엇을 바꿨더니(입력) → 얼마나 좋아졌나(측정)" 가 한 시간축에 있어야 보고 서사가 됨.
- **개선 결과**: 반영 1회마다 전후 기록 자동(수동 기록 0). 최근 40행 표시.

### 7. 표시 방식 — 구간마다 문구 대신 첫 슬라이드 범례 (2026-09-19)

- **개선 방법**: 표현을 "특허검토필요" → **"출원검토필요"** 로, 구간마다 붙이던 【문구】 대신 첫 슬라이드에 색상 범례 상자 하나.
  문구는 흑백 인쇄용 선택 옵션으로만(기본 해제). 예전 문구가 든 파일도 검토 반영이 알아봄(`LEGACY_*`).
- **개선된 부분**: `app/mark.py`, `config.py`, `review.py`, 화면 옵션, 문서, 예시 결과.
- **개선 이유**: 구간마다 문구가 붙으면 슬라이드 레이아웃이 깨지고 읽기 어려움.
- **개선 결과**: 원본에 덧붙는 요소 — 구간마다 1개 → **문서당 1개**(범례). 형광펜 색이 곧 등급.

### 8. 예시 선별 개선·교정 데이터 보호 (2026-09-20 새벽)

- **개선 방법**: 프롬프트에 주입하는 예시 칸을 반례 4 + 정답 8 로 늘리고 반영 단위(batch)로 선별, 보관 상한을 없앰.
  깨진 데이터 파일은 덮어쓰지 않고 보존. [보고용 표 복사]의 HTML 엔티티·표 구분자 처리.
- **개선된 부분**: `review.py` 선별 로직, 화면 복사 기능, 회귀 점검.
- **개선 이유**: 예시가 12개에서 멈춰 이후 교정이 반영되지 않았고, 파일이 깨지면 쌓은 교정이 사라질 위험.
- **개선 결과**: 보관 상한 12 → 무제한, 주입 12(반례 4·정답 8). 손상 시 데이터 손실 0(복구 기록이 타임라인에 뜸).

### 9. 업데이트 절차 [7-1], Windows 콘솔 출력 안정화 (2026-09-20 새벽)

- **개선 방법**: 이미 설치된 PC 에 새 버전을 덮어쓰는 절차를 `docs/00` [7-1] 로 문서화(백업·완전히 끄기·중첩 폴더 주의·확인 명령).
  점검 스크립트 출력을 utf-8 로 고정.
- **개선 이유**: USB 를 못 쓰게 되어 회사 PC 는 GitHub ZIP 으로만 갱신 → 절차가 문서 안에 있어야 함. cp949 콘솔에서 ✗ 같은 글자로 죽던 문제.
- **개선 결과**: 업데이트 5분 절차 + 확인 명령 1줄. 콘솔 인코딩 오류 0.

### 10. 산출물 간소화 (2026-09-20)

- **개선 방법**: 배지·마킹 요약 슬라이드·발표자 노트를 없애고, 산출물은 **형광펜 + 첫 슬라이드 범례**만. C 등급을 "공개 관련정보" 로 재정의.
- **개선된 부분**: `mark.py`, `merge.py`, `config.py`, 화면, 문서, 예시 결과.
- **개선 이유**: 원본을 최대한 건드리지 않아야 검토자가 받아들이기 쉽고, 검토 반영 때 비교도 단순해짐.
- **개선 결과**: 원본 변경 요소 4종 → 2종. 슬라이드 수 변화 0(요약 슬라이드 추가 안 함).

### 11. 파일 입력 4가지 + mark.bat (2026-09-21 00시 무렵) — 버전 표시 이전

- **개선 방법**: 서버가 브라우저와 같은 PC 에서 돌므로 파이썬이 디스크에서 직접 읽게 함.
  ① `input\` 폴더 목록 ② 경로 붙여넣기 ③ [찾기…] 파일 선택창 ④ 기존 업로드, + `mark.bat`(끌어다 놓기 → 명령행).
  결과는 항상 `output\이름_특허마킹.pptx`. 경로 API 는 화면에 심은 토큰이 있어야 응답.
- **개선된 부분**: `app/local.py`·`pipeline.py`·`cli.py`·`pickdialog.py`(신설), `main.py`, 화면, `mark.bat`, `docs/00` [7-2], 점검 38건.
- **개선 이유**: 회사 PC 는 브라우저 파일 업로드가 막혀 있음.
- **개선 결과**: 입력 통로 1 → 4 + 명령행. 웹/명령행이 같은 파이프라인을 써 결과 동일. 점검 38건 통과.

### 12. setup.bat 회사망 대응 (2026-09-21 오전)

- **개선 방법**: pip 설치가 SSL 오류로 실패하면 `--trusted-host` 로 자동 재시도, pip 네트워크 대기 15초 → 120초.
- **개선 이유**: 회사 보안 프로그램(SSL 검사)·느린 프록시에서 설치가 중간에 멈춤.
- **개선 결과**: 실패 시 자동 재시도 1회, 대기 15 → 120초. (설치 성공 여부는 회사 PC 에서 확인)

### 13. 슬라이드당 10분 제한 제거 (2026-09-21 오전) — 버전 2026-09-21

- **개선 방법**: 고정 시간 제한(600초)을 없애고 대신 ▸ 답을 **스트리밍**으로 받아 글자 수·경과 시간을 화면에 표시
  ▸ [중단] 이 모델 답변 도중에도 소켓을 끊어 즉시 멈춤 ▸ 15초마다 서버 생존 확인, 3회 연속 연결 거부면 자동 중단.
  `PM_TIMEOUT=초` 는 "무응답 제한" 으로 의미 변경.
- **개선된 부분**: `analyze._chat`(http.client 스트리밍 + 감시 스레드), `pipeline.py`, `cli.py`, `config.py`, 점검 9건(`test_model_call.py`), `docs/00` J.
- **개선 이유**: 회사 PC(CPU 만) 에서 슬라이드 하나가 10분을 넘겨 `TimeoutError: timed out` 으로 분석이 죽음.
- **개선 결과 (측정, 가짜 Ollama 40초 지연)**: 제한 600초 → 없음. [중단] 반응 — 예전엔 답이 올 때까지(최대 40초) 대기 → **약 3초**.
  서버 사망 감지 ≤ 45초(15초 × 3). 남는 리스크: 살아 있으나 멈춘 Ollama·스왑에 빠진 PC 는 자동 감지 못 함(화면 카운터로 사람이 판단).

### 14. 버전 표시·첫 화면 캐시 금지·오류 문구에 단계/버전 (2026-09-21 오후) — 버전 2026-09-21b

- **개선 방법**: `config.VERSION` 을 화면 제목 옆·검은 창 첫 줄·mark.bat·오류 문구 끝에 표시. 첫 화면 `Cache-Control: no-store`.
  작업 오류 문구에 "(단계: … · 버전 …)" 을 붙임.
- **개선 이유**: 업데이트 뒤에도 옛 프로그램이 돌고 있던 일이 반복됨(서버 창 안 닫음·중첩 폴더). 회사 테스트는 하루 1회라 한 번의 오판이 하루를 잃음.
- **개선 결과**: 버전 확인 — 테스트 명령 1분 → **화면 즉시**. 오류 보고 한 줄로 단계·버전이 함께 전달됨.

### 15. 모델 답 다이어트 + 속도 기록 + 특허 행정 오탐 필터 (2026-09-21 저녁) — 버전 2026-09-21c

- **개선 방법**:
  1. 모델이 쓰는 JSON 을 줄임 — 키를 한 글자(i·q·g·c·d·r)로, 사유 12자 이내, 인용구 25자 이내,
     `implicit`(묵시 여부)은 모델에게 묻지 않고 분류로 정함(`config.IMPLICIT_CATEGORIES`), 같은 문단·같은 유형은 1건, C 는 반환 안 함.
     옛 형식 답도 그대로 읽음(`_norm_item`) — 성능 측정의 "최초 설정" 과 쌓인 데이터 호환.
  2. 슬라이드마다 "입력/출력 토큰 · 쓰기 토큰/초" 를 화면·검은 창에 표시하고, 파일마다 `review_data\run_log.tsv` 에 한 줄 기록.
     결과 화면에 "인용구 일치 N/M"(모델 인용구가 원문과 정확히 맞아 자리를 잡은 비율)도 표시.
  3. 모델이 "특허 출원 0건·검토 미착수" 같은 특허 행정 상태를 공개 정보로 오인해 돌려보내는 것을 병합 단계에서 제거(`lexicon.is_ip_status`).
     프롬프트로 금지해도 어겨서, 규칙 사전의 판단(공개 신호 없음)과 겹칠 때만 결정적으로 뺌.
- **개선된 부분**: `analyze.py`(SCHEMA·SYSTEM 출력 규칙·`_norm_item`·`new_stats`), `merge.py`, `lexicon.py`, `pipeline.py`, `main.py`, `cli.py`, 화면,
  `tests/mock_llm.py`(축약 형식), `test_filter.py` +6, `test_local_input.py` +8.
- **개선 이유**: 회사 PC 에서 "너무 느리다". 실측 결과 시간의 **91%가 모델이 답을 쓰는 시간**이었고, 한 건 약 120 토큰 중 절반이
  키 이름·상투적 사유("수치적 범위가 제시되어…" 반복)·중복 플래그였음. 모델을 낮추기 전에 공짜로 줄일 수 있는 부분.
- **개선 결과 (측정 — 예시 문서 5장 · qwen3:8b · 같은 날 같은 Mac(M4) 에서 옛/새 프롬프트 번갈아 실행)**:

  | 지표 | 개선 전 (2회 측정) | 개선 후 | 변화 |
  |---|---|---|---|
  | 모델 출력 토큰 | 4,686 / 4,643 | **2,538** | **−45%** |
  | 답 쓰는 시간 | 489초 / 502초 | **265초** | **−47%** |
  | 한 건당 출력 토큰 | 120 / 125 | **69** | −45% |
  | 인용구 평균 길이 | 44자 (최대 86) | 23자 | 형광펜이 핵심 어구에 붙음 |
  | 사유 평균 길이 | 44자 | 8자 | 상투구 제거 |
  | 모델 판정 건수 | 37 | 37 (+규칙 사전 1) | 탐지량 유지 |
  | 특허 행정 상태 오탐 | 3건 | **0건** | 필터 |
  | 총 소요 (5장) | 약 535초 | 295초 | −45% |

  토큰 수는 PC 와 무관하게 같으므로, CPU 만 있는 회사 PC 에서도 **쓰기 시간이 같은 비율(약 절반)로 줄 것**으로 봅니다(추정).
  실측은 회사 PC 의 `run_log.tsv` 에서 버전 2026-09-21c 이전/이후 줄을 비교.
  (같은 문서를 두 번 돌려도 건수가 ±2 정도 흔들립니다 — temperature 0.1 에서의 정상 변동.)

### 16. 화면 모델 선택 — 정밀(qwen3:8b) / 빠름(qwen3:4b-instruct) (2026-09-21 밤) — 버전 2026-09-21d

- **개선 방법**: [문서 분석] 옵션 줄에 "모델" 선택 상자. 서버가 `/api/health` 로 선택지와 **설치 여부**를 내려 주고 설치된 것만 고를 수 있음.
  고른 모델이 없으면 시작 전에 "ollama pull …" 안내. 명령행 `--fast`. `setup.bat`/`setup.sh` 가 빠름 모델도 내려받음(실패해도 설치는 계속).
  `model_available()` 을 태그까지 정확히 맞추도록 고침(예전엔 qwen3:8b 가 있으면 qwen3:4b 도 있다고 봤음).
- **개선된 부분**: `config.MODEL_CHOICES`·`FAST_MODEL`, `main.py`(health choices·시작 전 검사), `analyze.model_available`, `cli.py --fast`,
  화면(`#optModel`), `setup.bat`·`setup.sh`, `docs/00` [5]·Q, 점검 4건.
- **개선 이유**: 속도가 급한 문서가 있고, 15회차 측정으로 4b 의 장단점이 분명해져 "문서마다 고르게" 하는 것이 낫다고 판단.
  기본 모델을 바꾸지 않은 이유는 위 "다음 개선 후보 — qwen3:4b" 의 측정 표(후보 1/3 감소, A 등급 없음).
- **개선 결과 (측정, 15회차와 같은 조건)**: 빠름 모드 총 소요 171초 → 49~55초(**3.3배**), 출력 토큰 2,522 → 1,167~1,214.
  대가: 후보 38~40 → 26, 수치·범위한정 후보 9~12 → 4, A 등급 3 → 0. 정밀 모드는 변화 없음.

---

## 측정 방법 (보고서에 숫자를 넣을 때)

### 속도 — `review_data\run_log.tsv`
분석이 끝날 때마다 한 줄이 쌓입니다(탭 구분, 엑셀에서 바로 열림). 열:

| 열 | 뜻 |
|---|---|
| 일시 · 버전 · 모델 | 언제, 어느 버전, 어느 모델로 돌렸나 |
| 파일 · 슬라이드 · 모델호출 | 문서 이름, 장 수, 모델을 부른 횟수 |
| 입력토큰 · 출력토큰 | 모델이 읽은 양 · 쓴 양 (Ollama 가 센 값) |
| 읽기초 · 쓰기초 · **쓰기토큰/초** | 읽는 데 걸린 초 · 쓰는 데 걸린 초 · **이 PC 의 실제 속도** |
| 총소요초 | 문서 하나의 전체 시간 |
| 후보 · A · B · C | 결과 건수 |
| **인용일치** | 모델 인용구가 원문과 정확히 맞은 비율 (낮으면 문단 전체가 칠해짐 — 품질 신호) |

보고에 쓰는 법: 같은 문서를 개선 전 버전과 후 버전으로 돌린 두 줄을 골라 "출력토큰·쓰기초·총소요초" 를 나란히 놓습니다.
문서가 다르면 장 수·글자 수가 달라 비교가 안 되니 **같은 문서**로 비교하세요. 예시 문서(`samples\회사보고자료_예시.pptx`)를 기준 문서로 두면 편합니다.

### 정확도 — [성능 기록] 탭
검토완료본이 쌓인 뒤 [성능 측정 실행] → 최초 설정 vs 현재 설정의 재현율·정밀도·F1·등급일치와 Δ. [보고용 표 복사] 로 마크다운 표.
자세한 해석은 `docs/04_성능_측정.md`.

### 보고용 표 서식 (이 문서의 각 회차를 그대로 옮기면 됩니다)

| 개선 방법 | 개선된 부분 | 개선 이유 | 개선 결과 (정량) |
|---|---|---|---|
| (한 줄) | (파일·화면) | (문제·요구) | (전후 숫자, 측정/추정 표기) |

---

## 앞으로의 기록 규칙

1. 도구를 바꿔 저장소에 올릴 때마다 **이 문서에 회차를 하나 추가**하고 `app/config.py` 의 `VERSION` 을 올린다(같은 날 두 번째면 b, c…).
2. "개선 결과" 는 반드시 숫자로. 재지 못했으면 "미측정 — 무엇으로 잴지" 를 적고, 회사 PC 에서 잰 뒤 채운다.
3. 속도는 `run_log.tsv`, 정확도는 [성능 기록] 탭 — 두 원천 밖의 숫자는 "추정" 으로 표기한다.

## 다음 개선 후보 (아직 안 한 것)

- **qwen3:4b 전환** — 보류 (2026-09-21 측정 결과 아래). 회사 검토 데이터가 쌓이면 [성능 기록] A/B(`set PM_MODEL=qwen3:4b-instruct` 로 모델만 바꿔 측정)로 재판단.

  **측정 (2026-09-21, 예시 5장, 참고 기준 = `samples/회사보고자료_예시_마킹결과.pptx` 의 형광펜 34구간, 모델별 2회, Mac M4):**

  | | qwen3:8b (현재) | qwen3:4b-instruct (2507) |
  |---|---|---|
  | 후보 수 | 38 / 40 (모델 판정 37~39) | 26 / 26 (모델 판정 16~17 + 규칙 사전 구제 9~10) |
  | 재현율 (참고 기준 대비) | 70.6% / 73.5% | 67.6% / 67.6% |
  | 정밀도 | 84.4% / 84.8% | 100% / 100% |
  | F1 | 76.9 / 78.8 | 80.7 / 80.7 |
  | A 등급(수단이 드러난 후보) | 3건 | **0건** — 전부 B |
  | 수치·범위한정 후보 | 9~12건 | 4건 |
  | 8b 후보 중 4b 도 잡은 비율 | — | 25/38 = 66% |
  | 총 소요 (Mac) | 171초 | 49~55초 (**3.3배 빠름**) |
  | 출력 토큰 | 2,522~2,527 | 1,167~1,214 |

  읽는 법: 참고 기준은 사람이 검증한 정답이 아니라 예전 판정 목록을 복원한 것이라 절대값보다 **두 모델의 차이**를 본다.
  4b 는 잡는 것은 정확하지만(정밀도 100%) **후보를 1/3 덜 잡고**, 특히 수치·범위한정 세부 후보가 1/3 로 줄며 A/B 등급 구분을 못 한다.
  "빠짐없이 추린다"는 이 도구의 원칙(재현율 우선)과 반대 방향이라 기본 모델로는 부적합. 속도가 급한 문서의 '초벌' 용도로는 쓸 만하다
  (CPU 회사 PC 에서는 토큰이 절반·토큰당 속도 약 2배라 **3~4배** 빠를 것으로 추정).
- **인용구 불일치** — 예시 문서에서 37건 중 5건이 원문과 정확히 안 맞아 문단 전체가 칠해짐. 공백·기호 차이를 눌러 맞추는 보정 검토.
- **'전체 문단 검사' 기본값** — 끄면 읽기 시간이 줄지만 사전에 없는 표현을 놓침. 회사 문서에서 규칙 사전 적중률을 본 뒤 판단.
- **경영 지표 오탐 필터의 문맥** — 인용구가 짧아지면서 필터가 보는 글자도 줄었음. 같은 문장 안의 경영 단어까지 보게 할지 검토.
- **RAM 32GB 증설 시** — `qwen3:30b-a3b`(품질 14b급, 속도 3b급) 가 후보. 16GB 에서는 안 올라감.
